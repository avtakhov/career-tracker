{% extends "layout.html" %}
{% block content %}
<div class="col-12">
<div class="card">
    <div class="card-header">
    <h3 class="card-title">Attendance</h3>
    </div>
    <div class="card-body border-bottom py-3">
        <form method="POST">
            {{ event_form.select(class="mb-3 form-control") }}
            {{ event_form.submit(class="mb-3 btn") }}
        </form>
    </div>
</div>
</div>
{% endblock %}